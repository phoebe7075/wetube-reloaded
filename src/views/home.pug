extends base.pug
include mixins/video 

block content 
    div.videos__grid
        each video in videos
            +video(video)
            +video(video)
            +video(video)
            +video(video)
        else 
            span.empty__message No videos found