mixin video(video)
    div 
        h4 
            a(href=`/videos/${video.id}`)=video.title /* #{}는 attribute에 사용 불가 */
        ul 
            li #{video.meta.rating}/5 
            li #{video.description} 
            li Posted #{video.createdAt}. 
            li #{video.meta.views} views.